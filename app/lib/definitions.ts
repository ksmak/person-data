export type User = {
    id: number,
    isActive: boolean,
    login: string,
    password: string,
    firstName: string,
    lastName: string,
    middleName: string,
}

export type CustomerField = {
    id: string;
    name: string;
};