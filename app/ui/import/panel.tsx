import { Person } from "@/app/lib/definitions";

export default function ImportPanel({
    persons
}: { persons: Person[] }) {

    return (
        // {persons.map((person: Person) => (
        <div>1</div>
        // ))}
    )
}